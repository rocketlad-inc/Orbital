# 2g was enough when the only module was a Java TWA. The wear module
# adds Kotlin and the Compose compiler plugin, which is where the
# "Daemon will be stopped, JVM heap space exhausted" on a CI runner
# comes from.
org.gradle.jvmargs=-Xmx4096m
android.useAndroidX=true
android.nonTransitiveRClass=true
