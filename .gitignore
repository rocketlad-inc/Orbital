# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*~

# Node (in case tooling is added later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build artifacts
dist/
build/
.cache/

# Local env
.env
.env.local

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example

# Claude Code session artifacts
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/agents/
# Auto-generated build SHA (scripts/bundle-migrations.js)
src/_version.ts

# curl cookie jars from local API testing (contain session tokens)
cookie.txt
judge.txt
super.txt
*.cookies

# Agent credentials — NEVER commit. .agent.local holds either the
# AGENT_KEY minting secret or a session token; .agent.session is the
# cached session scripts/agent.mjs mints from it.
.agent.local
.agent.session
